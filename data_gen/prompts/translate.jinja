{% if prior_context %}
For context only — this is the end of the text that comes immediately before what you're translating. Do NOT translate it and do NOT include it in your output; it's shown only so you understand what's being continued:
---
{{ prior_context }}
---
{% endif %}
Translate the text below to {{ target_language }}. Rules:
- Translate the ENTIRE text completely — do not omit, summarize, or add content.
- Keep placeholder tokens of the form ⟦N⟧ (e.g. ⟦0⟧, ⟦1⟧) exactly as they are — do not translate, remove, or alter them.
- Preserve markdown formatting (headings, **bold**, bullet points, line breaks) as in the source.
- Only actual programming code or pseudocode (syntax from a programming language — loops, assignments, function definitions) may be left untranslated. Everything else — including sentences that contain math, equations, symbols, or placeholder tokens — is natural-language prose and must be translated like any other sentence; only the code/equation/placeholder itself stays as-is, never the words around it.
- Translate every sentence and every line of the input, in the same order. Never shorten the response to a subset of the input, a single line, or a summary — the output's structure must mirror the input's.
- This text is a stream-of-consciousness reasoning trace, not formal writing. Casual interjections and discourse markers (words like "wait", "hmm", "actually", "let me check") should be translated the way a person would actually say them out loud in {{ target_language }} — a natural spoken interjection with the same conversational function — never a stiff, literal dictionary translation of the word in isolation.
- Output ONLY the {{ target_language }} translation. No preamble, no notes, no explanations.
