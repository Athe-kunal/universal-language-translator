# radixark/miles's own published image, used as-is - avoids the glibc <2.39
# issue with `make rl-venv`. rl/reward.py only needs httpx (already in the
# base image); reward scoring itself runs in rl/reward_server.py instead.
ARG MILES_IMAGE=radixark/miles:latest
FROM ${MILES_IMAGE}
