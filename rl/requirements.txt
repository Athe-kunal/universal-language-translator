# Extra dependencies for rl/reward.py, installed into the miles venv
# alongside `miles-rl` and sglang - see the Makefile's rl-venv target and
# rl/run_qwen3_0_6b_bpcc_fsdp.py's module docstring for why this is a
# separate venv from the project's main one (transformers version conflict).
#
# jina-embeddings-v3 (rl/reward.py's similarity model, via
# data_gen/embeddings.py) has only been validated in this project's main
# venv (transformers<5.0 - see reward_metric_experiment.md). It has not been
# verified against the transformers==5.x miles pins; if it fails to load
# here, either pin an older transformers in this venv (may conflict with
# miles/sglang) or run the reward as a small service in the main venv and
# have rl/reward.py call it over HTTP instead of loading the model in-process.
sentence-transformers>=5.7.0
einops>=0.8.2
loguru>=0.7.3
